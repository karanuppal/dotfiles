[alias]
    # List aliases
    la = "!git config -l | grep alias | cut -c 7-"

    # Nice, colorful graph visualization
    lg = log --decorate --oneline --graph --branches --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold blue)(%ar) %C(bold yellow)%d%C(reset) %C(green)%s%C(reset) %C(dim green)- %an%C(reset)'
    logdog = log --decorate --oneline --graph

[color]
    ui = true
    status = true
    branch = true
    diff = true
[core]
    pager = less -R
    preloadindex = true
[mergetool "fugitive"]
    cmd = vim -f -c \"Gvdiff\" \"$MERGED\"
[mergetool]
    keepBackup = false
[merge]
    tool = fugitive
[push]
    default = simple
[init]
    templatedir = ~/.git_template
[grep]
    lineNumber = true

[hub]
    host = bbgithub.dev.bloomberg.com
    protocol = https
[http]
    proxy = devproxy.bloomberg.com:82
    sslVerify = false

[http "https://bbgithub.dev.bloomberg.com"]
    proxy = https://bproxy.tdmz1.bloomberg.com:80
    sslVerify = false
[user]
	email = kuppal8@bloomberg.net
	name = Karan Uppal
